{
  "name": "my-scraper-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postinstall": "npx playwright install",  // Ensures browsers are installed after every install
    "install": "npx playwright install"       // Ensures browsers are installed when running npm install
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.4",
    "next": "^15.3.1",
    "playwright": "^1.41.0",  // Make sure Playwright is installed
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "tailwindcss": "^4.1.4"
  }
}

